[package]
name = "rental-escrow"
version = "0.1.0"
edition = "2021"
rust-version = "1.91.0"
publish = false

[lib]
crate-type = ["cdylib", "rlib"]
doctest = false

[dependencies]
soroban-sdk = "=27.0.5"

[dev-dependencies]
soroban-sdk = { version = "=27.0.5", features = ["testutils"] }
